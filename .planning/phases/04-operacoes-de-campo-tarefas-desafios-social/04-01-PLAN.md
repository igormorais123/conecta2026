---
phase: 04-operacoes-de-campo-tarefas-desafios-social
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "C:\\Users\\IgorPC\\.claude\\projects\\Conecta 2026\\js\\elexion-client.js"
  - "C:\\Users\\IgorPC\\.claude\\projects\\Conecta 2026\\CONECTA.html"
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07, OPS-08]

must_haves:
  truths:
    - "Sidebar do CONECTA exibe itens 'Tarefas de Campo', 'Desafios' e 'Social' na secao Campo, abaixo de Gamificacao"
    - "Secao Tarefas de Campo lista tarefas Elexion com filtros de status e tipo — sem confundir com tarefas Supabase"
    - "Modal de criacao de tarefa permite titulo, descricao, tipo, localizacao, deadline e atribuicao a cabo"
    - "POST /api/v1/tasks e POST /api/v1/tasks/:id/assign sao chamados via ElexionClient"
    - "Reports de tarefas concluidas sao exibidos em painel dedicado com resultado e fotos"
    - "Secao Desafios lista desafios ativos com progresso e leaderboard por desafio"
    - "Modal de criacao de desafio permite tipo, metrica, alvo, recompensa XP e duracao"
    - "Secao Social exibe scorecard individual por cabo (posts, engagement, plataformas)"
    - "Quando Elexion offline, todas as tres secoes exibem fallback sem quebrar o CONECTA"
  artifacts:
    - path: "C:\\Users\\IgorPC\\.claude\\projects\\Conecta 2026\\js\\elexion-client.js"
      provides: "Metodos de escrita POST adicionados: createTask, assignTask, createChallenge, fetchTasks, fetchTaskReports, fetchChallenges, fetchChallengeProgress, fetchChallengeLeaderboard, fetchSocialTeamMetrics, fetchSocialScorecard"
      exports: ["createTask", "assignTask", "createChallenge", "fetchTasks", "fetchTaskReports", "fetchChallenges", "fetchChallengeProgress", "fetchChallengeLeaderboard", "fetchSocialTeamMetrics", "fetchSocialScorecard"]
    - path: "C:\\Users\\IgorPC\\.claude\\projects\\Conecta 2026\\CONECTA.html"
      provides: "Secoes page-tarefas-campo, page-desafios, page-social com HTML, CSS e JS inline"
      contains: "page-tarefas-campo"
  key_links:
    - from: "sidebar nav-item tarefas-campo"
      to: "page-tarefas-campo"
      via: "onclick=\"showPage('tarefas-campo')\""
      pattern: "showPage.*tarefas-campo"
    - from: "page-tarefas-campo"
      to: "ElexionClient.createTask()"
      via: "salvarTarefaCampo() chamado no submit do modal"
      pattern: "salvarTarefaCampo"
    - from: "page-desafios"
      to: "ElexionClient.createChallenge()"
      via: "salvarDesafio() chamado no submit do modal"
      pattern: "salvarDesafio"
    - from: "page-social"
      to: "ElexionClient.fetchSocialScorecard()"
      via: "renderSocial() chamado em showPage e polling 60s"
      pattern: "renderSocial"
---

<objective>
Adicionar tres secoes ao CONECTA.html — "Tarefas de Campo", "Desafios" e "Social" — que consomem a API Elexion para operacoes de escrita e leitura de metricas sociais. Estende o ElexionClient com metodos POST para criacao de tarefas e desafios.

Purpose: Coordenadores precisam criar tarefas gamificadas e desafios diretamente do CONECTA, sem acessar o painel Elexion separado. As metricas sociais fecham o loop de visibilidade sobre o que cabos fazem online.

Output:
- js/elexion-client.js com 10 novos metodos (leitura + escrita de tasks, challenges, social)
- CONECTA.html com 3 novas secoes (HTML + CSS + JS), 3 novos nav-items na secao Campo
- 2 novos modais (criacao tarefa, criacao desafio)
- Polling 60s nas 3 secoes, fallback gracioso quando Elexion offline
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Contrato atual do ElexionClient (js/elexion-client.js). Nao alterar metodos existentes. -->
<!-- Apenas ADICIONAR os metodos abaixo ao final da lista de endpoints, antes do return {}. -->

```javascript
// Metodos JA EXISTENTES — nao tocar:
ElexionClient.login(email, password)
ElexionClient.logout()
ElexionClient.isAuthenticated()
ElexionClient.getCurrentUser()
ElexionClient.request(path, options)       // fetch autenticado com retry 401
ElexionClient.fetchKpis()
ElexionClient.fetchHeatmap()
ElexionClient.fetchHeatmapGaps()
ElexionClient.fetchGeofences()
ElexionClient.fetchWarRoomFeed()
ElexionClient.fetchAlerts()
ElexionClient.fetchLeaderboard(equipeId?)
ElexionClient.fetchTeamsRanking()

// ADICIONAR — metodos de leitura de tarefas:
ElexionClient.fetchTasks(filters?)
// -> GET /tasks?status={status}&type={type}&page=1&limit=20
// filters: { status?: 'pending'|'in_progress'|'completed'|'overdue', type?: string }
// Retorna: { data: Task[], total: number, page: number } | null

ElexionClient.fetchTaskReports(taskId)
// -> GET /tasks/:id/reports
// Retorna: array de { id, result, photos: string[], completedAt, workerId, workerName } | null

// ADICIONAR — metodos de escrita de tarefas:
ElexionClient.createTask(payload)
// -> POST /tasks com body: { title, description, type, location, deadline, xpReward }
// payload obrigatorio: { title: string, type: string }
// payload opcional: { description?, location?, deadline?: ISO8601, xpReward?: number }
// Retorna: Task criada ou null

ElexionClient.assignTask(taskId, workerId)
// -> POST /tasks/:id/assign com body: { workerId }
// Retorna: { success: true } | null

// ADICIONAR — metodos de desafios:
ElexionClient.fetchChallenges()
// -> GET /challenges?status=active
// Retorna: array de Challenge | null

ElexionClient.createChallenge(payload)
// -> POST /challenges com body: { type, metric, target, xpReward, durationDays }
// payload: { type: string, metric: string, target: number, xpReward: number, durationDays: number }
// Retorna: Challenge criado ou null

ElexionClient.fetchChallengeProgress(challengeId)
// -> GET /challenges/:id/progress
// Retorna: { challengeId, participants: [{workerId, workerName, current, target, pct}] } | null

ElexionClient.fetchChallengeLeaderboard(challengeId)
// -> GET /challenges/:id/leaderboard
// Retorna: array de { posicao, nome, xp, progresso } | null

// ADICIONAR — metodos de social:
ElexionClient.fetchSocialTeamMetrics(teamId)
// -> GET /social/team/:teamId/metrics
// teamId: string (id da equipe regional)
// Retorna: { teamId, posts: number, engagement: number, reach: number, platforms: {...} } | null

ElexionClient.fetchSocialScorecard(userId)
// -> GET /social/scorecard/:userId
// userId: string (id do cabo)
// Retorna: { userId, name, posts, engagement, reach, platforms: {instagram,tiktok,x} } | null
```

<!-- Padrao de navegacao do CONECTA.html -->
// showPage(page) — ativa .page#page-{name} e o nav-item com onclick contendo '{name}'
// titles{} dentro de showPage() precisa receber as 3 novas chaves:
//   'tarefas-campo': 'Tarefas de Campo (Elexion)',
//   'desafios': 'Desafios de Campo',
//   'social': 'Metricas Sociais dos Cabos'
// Nav-items usam onclick inline: onclick="showPage('tarefas-campo')"
// Pages sao divs: <div class="page" id="page-tarefas-campo">

<!-- Variaveis CSS disponiveis (nao redefinir) -->
// --primary: #1a237e | --accent: #ff6f00 | --success: #2e7d32 | --danger: #c62828
// --warning: #f57f17 | --info: #0277bd | --bg: #f0f2f5 | --card-bg: #ffffff

<!-- Estrutura de modal existente (reutilizar) -->
// <div class="modal-overlay" role="dialog" aria-modal="true" id="modalXxx" onclick="closeModal('modalXxx')">
//   <div class="modal" onclick="event.stopPropagation()">
//     <h2>TITULO EM UPPERCASE</h2>
//     <div class="form-group"><label>Campo</label><input ...></div>
//     <div class="modal-actions">
//       <button class="btn btn-outline" onclick="closeModal('modalXxx')">Cancelar</button>
//       <button class="btn btn-primary" onclick="salvarXxx()">Salvar</button>
//     </div>
//   </div>
// </div>

<!-- Tarefas Supabase (NAO CONFUNDIR) -->
// O CONECTA ja tem um sistema de tarefas baseado em Supabase:
// - chave localStorage: 'conectacelina_tarefas_pessoas'
// - overlay: id="tarefasOverlay" com classe .tarefas-overlay
// - funcoes: openTarefasPessoa(), closeTarefasPessoa(), addTarefaPessoa()
// As novas secoes usam prefixo "campo" (ids, funcoes, storage keys) para evitar conflito.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender ElexionClient com metodos de escrita e leitura de operacoes</name>
  <files>js/elexion-client.js</files>
  <action>
Adicionar 10 novos metodos ao ElexionClient existente. NAO alterar nenhum metodo ja existente.

Localizar o bloco `// ---- API publica ----` e o `return { ... }` final. Inserir os novos metodos ANTES do return, e adicionar todos os 10 nomes ao objeto retornado.

Implementar cada metodo seguindo o padrao da funcao `request(path, options)` ja existente (retorna null em erro, nao propaga excecao):

```javascript
/** GET /api/v1/tasks — lista tarefas de campo paginadas */
async function fetchTasks(filters = {}) {
  const params = new URLSearchParams();
  if (filters.status) params.set('status', filters.status);
  if (filters.type) params.set('type', filters.type);
  params.set('page', '1');
  params.set('limit', '20');
  return request('/tasks?' + params.toString());
}

/** GET /api/v1/tasks/:id/reports */
async function fetchTaskReports(taskId) {
  return request('/tasks/' + encodeURIComponent(taskId) + '/reports');
}

/** POST /api/v1/tasks — requer role COORDINATOR */
async function createTask(payload) {
  return request('/tasks', {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

/** POST /api/v1/tasks/:id/assign */
async function assignTask(taskId, workerId) {
  return request('/tasks/' + encodeURIComponent(taskId) + '/assign', {
    method: 'POST',
    body: JSON.stringify({ workerId }),
  });
}

/** GET /api/v1/challenges?status=active */
async function fetchChallenges() {
  return request('/challenges?status=active');
}

/** POST /api/v1/challenges — requer role COORDINATOR */
async function createChallenge(payload) {
  return request('/challenges', {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

/** GET /api/v1/challenges/:id/progress */
async function fetchChallengeProgress(challengeId) {
  return request('/challenges/' + encodeURIComponent(challengeId) + '/progress');
}

/** GET /api/v1/challenges/:id/leaderboard */
async function fetchChallengeLeaderboard(challengeId) {
  return request('/challenges/' + encodeURIComponent(challengeId) + '/leaderboard');
}

/** GET /api/v1/social/team/:teamId/metrics */
async function fetchSocialTeamMetrics(teamId) {
  return request('/social/team/' + encodeURIComponent(teamId) + '/metrics');
}

/** GET /api/v1/social/scorecard/:userId */
async function fetchSocialScorecard(userId) {
  return request('/social/scorecard/' + encodeURIComponent(userId) + '/');
}
```

Atualizar o objeto `return { ... }` para incluir os 10 novos nomes: fetchTasks, fetchTaskReports, createTask, assignTask, fetchChallenges, createChallenge, fetchChallengeProgress, fetchChallengeLeaderboard, fetchSocialTeamMetrics, fetchSocialScorecard.
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const src = fs.readFileSync('js/elexion-client.js', 'utf8');
const required = ['fetchTasks','fetchTaskReports','createTask','assignTask','fetchChallenges','createChallenge','fetchChallengeProgress','fetchChallengeLeaderboard','fetchSocialTeamMetrics','fetchSocialScorecard'];
const missing = required.filter(m => !src.includes(m));
if (missing.length) { console.error('MISSING:', missing); process.exit(1); }
console.log('OK — todos os 10 metodos presentes');
"
    </automated>
  </verify>
  <done>elexion-client.js exporta todos os 10 novos metodos no objeto public. Metodos existentes inalterados. Cada metodo usa request() com fallback null.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar nav-items e secoes HTML/CSS para Tarefas de Campo, Desafios e Social</name>
  <files>CONECTA.html</files>
  <action>
Realizar 4 insercoes cirurgicas no CONECTA.html. Usar a estrutura exata dos elementos existentes como modelo.

**Insercao 1 — Nav-items (sidebar)**

Localizar o bloco:
```html
        <div class="nav-section">Campo</div>
        <div class="nav-item" onclick="showPage('gamificacao')">
```

Inserir APOS o nav-item de gamificacao (antes de `<div class="nav-section">Logistica</div>`):

```html
        <div class="nav-item" onclick="showPage('tarefas-campo')">
            <span class="nav-icon">&#9989;</span>
            <span>Tarefas de Campo</span>
        </div>
        <div class="nav-item" onclick="showPage('desafios')">
            <span class="nav-icon">&#127942;</span>
            <span>Desafios</span>
        </div>
        <div class="nav-item" onclick="showPage('social')">
            <span class="nav-icon">&#128241;</span>
            <span>Social</span>
        </div>
```

**Insercao 2 — Secoes HTML das 3 paginas**

Localizar `</div><!-- /page-gamificacao -->` e inserir logo APOS:

```html
    <!-- ========== TAREFAS DE CAMPO ========== -->
    <div class="page" id="page-tarefas-campo">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px;flex-wrap:wrap;gap:12px;">
            <div>
                <h2 style="font-size:1.4rem;font-weight:800;color:var(--primary);margin:0;">&#9989; Tarefas de Campo</h2>
                <p style="font-size:0.8rem;color:var(--text-light);margin:4px 0 0;">Tarefas gamificadas via Elexion &mdash; separado das tarefas internas do CONECTA</p>
            </div>
            <div style="display:flex;gap:10px;flex-wrap:wrap;">
                <select id="campo-filtro-status" onchange="renderTarefasCampo()" style="padding:8px 12px;border:2px solid #e0e0e0;border-radius:8px;font-size:0.8rem;font-family:inherit;">
                    <option value="">Todos os status</option>
                    <option value="pending">Pendente</option>
                    <option value="in_progress">Em andamento</option>
                    <option value="completed">Conclu&iacute;da</option>
                    <option value="overdue">Atrasada</option>
                </select>
                <select id="campo-filtro-tipo" onchange="renderTarefasCampo()" style="padding:8px 12px;border:2px solid #e0e0e0;border-radius:8px;font-size:0.8rem;font-family:inherit;">
                    <option value="">Todos os tipos</option>
                    <option value="panfletagem">Panfletagem</option>
                    <option value="visita_domiciliar">Visita domiciliar</option>
                    <option value="evento">Evento</option>
                    <option value="midia_social">M&iacute;dia social</option>
                </select>
                <button class="btn btn-primary" onclick="openModal('modalTarefaCampo')" style="font-size:0.8rem;">+ Nova Tarefa</button>
            </div>
        </div>
        <div id="campo-tarefas-lista" style="display:grid;gap:12px;"></div>
        <div id="campo-tarefas-empty" style="display:none;text-align:center;padding:40px;color:var(--text-light);">
            <p style="font-size:2rem;">&#9989;</p>
            <p>Nenhuma tarefa encontrada. Crie a primeira tarefa de campo.</p>
        </div>
        <div id="campo-tarefas-offline" style="display:none;text-align:center;padding:40px;background:#fff3e0;border-radius:12px;color:var(--warning);">
            <p>Elexion indispon&iacute;vel. Reconecte para ver tarefas de campo.</p>
        </div>
        <!-- Reports panel (visivel apos clicar em uma tarefa concluida) -->
        <div id="campo-reports-panel" style="display:none;margin-top:20px;">
            <h3 style="font-size:1rem;font-weight:700;color:var(--primary);margin-bottom:12px;">Reports da Tarefa</h3>
            <div id="campo-reports-lista" style="display:grid;gap:10px;"></div>
        </div>
    </div><!-- /page-tarefas-campo -->

    <!-- ========== DESAFIOS ========== -->
    <div class="page" id="page-desafios">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px;flex-wrap:wrap;gap:12px;">
            <div>
                <h2 style="font-size:1.4rem;font-weight:800;color:var(--primary);margin:0;">&#127942; Desafios de Campo</h2>
                <p style="font-size:0.8rem;color:var(--text-light);margin:4px 0 0;">Desafios semanais gamificados para cabos eleitorais</p>
            </div>
            <button class="btn btn-primary" onclick="openModal('modalDesafio')" style="font-size:0.8rem;">+ Novo Desafio</button>
        </div>
        <div id="desafios-lista" style="display:grid;gap:16px;grid-template-columns:repeat(auto-fill,minmax(320px,1fr));"></div>
        <div id="desafios-empty" style="display:none;text-align:center;padding:40px;color:var(--text-light);">
            <p style="font-size:2rem;">&#127942;</p>
            <p>Nenhum desafio ativo. Lance o primeiro desafio para a equipe.</p>
        </div>
        <div id="desafios-offline" style="display:none;text-align:center;padding:40px;background:#fff3e0;border-radius:12px;color:var(--warning);">
            <p>Elexion indispon&iacute;vel. Reconecte para ver desafios.</p>
        </div>
        <!-- Leaderboard do desafio selecionado -->
        <div id="desafio-leaderboard-panel" style="display:none;margin-top:24px;">
            <h3 id="desafio-lb-titulo" style="font-size:1rem;font-weight:700;color:var(--primary);margin-bottom:12px;">Leaderboard do Desafio</h3>
            <div id="desafio-lb-lista"></div>
        </div>
    </div><!-- /page-desafios -->

    <!-- ========== SOCIAL ========== -->
    <div class="page" id="page-social">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px;flex-wrap:wrap;gap:12px;">
            <div>
                <h2 style="font-size:1.4rem;font-weight:800;color:var(--primary);margin:0;">&#128241; M&eacute;tricas Sociais</h2>
                <p style="font-size:0.8rem;color:var(--text-light);margin:4px 0 0;">Posts, engajamento e alcance dos cabos por plataforma</p>
            </div>
            <span id="social-ultima-atualizacao" style="font-size:0.72rem;color:var(--text-light);"></span>
        </div>
        <!-- Cards de metricas agregadas por equipe -->
        <div id="social-equipes-lista" style="display:grid;gap:16px;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));margin-bottom:24px;"></div>
        <!-- Scorecard individual (expandido ao clicar em cabo no leaderboard da gamificacao) -->
        <div id="social-scorecard-panel" style="display:none;background:#fff;border-radius:12px;padding:20px;box-shadow:0 2px 8px rgba(0,0,0,0.08);margin-top:8px;">
            <h3 id="social-scorecard-nome" style="font-size:1rem;font-weight:700;color:var(--primary);margin-bottom:16px;"></h3>
            <div id="social-scorecard-content" style="display:grid;gap:12px;grid-template-columns:repeat(auto-fill,minmax(140px,1fr));"></div>
        </div>
        <div id="social-empty" style="display:none;text-align:center;padding:40px;color:var(--text-light);">
            <p>Configure as equipes e dados sociais no Elexion para ver m&eacute;tricas aqui.</p>
        </div>
        <div id="social-offline" style="display:none;text-align:center;padding:40px;background:#fff3e0;border-radius:12px;color:var(--warning);">
            <p>Elexion indispon&iacute;vel. Reconecte para ver m&eacute;tricas sociais.</p>
        </div>
    </div><!-- /page-social -->
```

**Insercao 3 — Modais**

Localizar o comentario `<!-- ===== MODAIS =====` e inserir os 2 novos modais logo APOS esse comentario, antes do primeiro modal existente (`<!-- Modal Evento -->`):

```html
<!-- Modal Tarefa de Campo (Elexion) -->
<div class="modal-overlay" role="dialog" aria-modal="true" id="modalTarefaCampo" onclick="closeModal('modalTarefaCampo')">
    <div class="modal" onclick="event.stopPropagation()">
        <h2>Nova Tarefa de Campo</h2>
        <div class="form-group">
            <label>T&iacute;tulo *</label>
            <input type="text" id="campo-task-titulo" placeholder="Ex: Panfletagem Ceilandia Norte">
        </div>
        <div class="form-group">
            <label>Tipo *</label>
            <select id="campo-task-tipo">
                <option value="panfletagem">Panfletagem</option>
                <option value="visita_domiciliar">Visita domiciliar</option>
                <option value="evento">Evento</option>
                <option value="midia_social">M&iacute;dia social</option>
                <option value="outro">Outro</option>
            </select>
        </div>
        <div class="form-group">
            <label>Descri&ccedil;&atilde;o</label>
            <textarea id="campo-task-descricao" placeholder="Detalhes da tarefa para o cabo..."></textarea>
        </div>
        <div class="form-group">
            <label>Localiza&ccedil;&atilde;o</label>
            <input type="text" id="campo-task-localizacao" placeholder="Ex: Pra&ccedil;a do Povo, Ceilandia">
        </div>
        <div class="form-group">
            <label>Prazo (deadline)</label>
            <input type="datetime-local" id="campo-task-deadline">
        </div>
        <div class="form-group">
            <label>Recompensa XP</label>
            <input type="number" id="campo-task-xp" placeholder="Ex: 100" min="0" max="9999">
        </div>
        <div class="form-group">
            <label>Atribuir ao Cabo (ID)</label>
            <input type="text" id="campo-task-worker-id" placeholder="ID do cabo no Elexion (opcional)">
        </div>
        <div class="modal-actions">
            <button class="btn btn-outline" onclick="closeModal('modalTarefaCampo')">Cancelar</button>
            <button class="btn btn-primary" id="campo-task-btn-salvar" onclick="salvarTarefaCampo()">Criar Tarefa</button>
        </div>
    </div>
</div>

<!-- Modal Desafio (Elexion) -->
<div class="modal-overlay" role="dialog" aria-modal="true" id="modalDesafio" onclick="closeModal('modalDesafio')">
    <div class="modal" onclick="event.stopPropagation()">
        <h2>Novo Desafio</h2>
        <div class="form-group">
            <label>Tipo de Desafio *</label>
            <select id="desafio-tipo">
                <option value="tarefas_semana">Mais tarefas na semana</option>
                <option value="xp_dia">Mais XP em um dia</option>
                <option value="visitas">Mais visitas domiciliares</option>
                <option value="posts">Mais posts nas redes</option>
            </select>
        </div>
        <div class="form-group">
            <label>M&eacute;trica *</label>
            <input type="text" id="desafio-metrica" placeholder="Ex: tasks_completed">
        </div>
        <div class="form-group">
            <label>Alvo (meta num&eacute;rica) *</label>
            <input type="number" id="desafio-alvo" placeholder="Ex: 10" min="1">
        </div>
        <div class="form-group">
            <label>Recompensa XP *</label>
            <input type="number" id="desafio-xp" placeholder="Ex: 500" min="1">
        </div>
        <div class="form-group">
            <label>Dura&ccedil;&atilde;o (dias) *</label>
            <input type="number" id="desafio-duracao" placeholder="Ex: 7" min="1" max="30" value="7">
        </div>
        <div class="modal-actions">
            <button class="btn btn-outline" onclick="closeModal('modalDesafio')">Cancelar</button>
            <button class="btn btn-primary" id="desafio-btn-salvar" onclick="salvarDesafio()">Criar Desafio</button>
        </div>
    </div>
</div>
```

**Insercao 4 — Titulos no showPage()**

Localizar o objeto `titles` dentro de `showPage()`:
```javascript
gamificacao: 'Gamificação de Campo'
```

Adicionar as 3 novas chaves logo apos:
```javascript
'tarefas-campo': 'Tarefas de Campo (Elexion)',
desafios: 'Desafios de Campo',
social: 'M\u00e9tricas Sociais dos Cabos',
```
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const src = fs.readFileSync('CONECTA.html', 'utf8');
const checks = [
  ['page-tarefas-campo', 'secao tarefas-campo'],
  ['page-desafios', 'secao desafios'],
  ['page-social', 'secao social'],
  ['modalTarefaCampo', 'modal tarefa campo'],
  ['modalDesafio', 'modal desafio'],
  [\"showPage('tarefas-campo')\", 'nav-item tarefas-campo'],
  [\"showPage('desafios')\", 'nav-item desafios'],
  [\"showPage('social')\", 'nav-item social'],
  ['tarefas-campo-elexion', 'titulo tarefas-campo'],
];
const missing = checks.filter(([s]) => !src.includes(s));
if (missing.length) { console.error('MISSING:', missing.map(([,n])=>n)); process.exit(1); }
console.log('OK — todas as insercoes presentes');
"
    </automated>
  </verify>
  <done>CONECTA.html tem 3 novos nav-items na secao Campo, 3 novas divs .page, 2 novos modais e os titulos registrados em showPage(). Nenhum elemento existente foi removido ou alterado.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar renderTarefasCampo, renderDesafios, renderSocial e funcoes de submit</name>
  <files>CONECTA.html</files>
  <action>
Adicionar bloco JavaScript ao final do `<script>` principal do CONECTA.html (antes do fechamento `</script>`). O bloco deve conter todas as funcoes de render e submit para as 3 novas secoes.

Localizar `// --- fim do script principal ---` ou o ultimo `</script>` do bloco inline de JS. Inserir o seguinte bloco imediatamente ANTES do fechamento:

```javascript
// ===== OPERACOES DE CAMPO (Phase 4) =====
// NOTA: "Tarefas de Campo" = Elexion API. "Tarefas" existentes = Supabase (nao confundir).

// ---- Tarefas de Campo ----

async function renderTarefasCampo() {
  const status = document.getElementById('campo-filtro-status')?.value || '';
  const tipo = document.getElementById('campo-filtro-tipo')?.value || '';
  const lista = document.getElementById('campo-tarefas-lista');
  const empty = document.getElementById('campo-tarefas-empty');
  const offline = document.getElementById('campo-tarefas-offline');
  if (!lista) return;

  const filters = {};
  if (status) filters.status = status;
  if (tipo) filters.type = tipo;

  const res = await ElexionClient.fetchTasks(filters);

  if (res === null) {
    lista.style.display = 'none';
    empty.style.display = 'none';
    offline.style.display = 'block';
    return;
  }

  offline.style.display = 'none';
  const tarefas = Array.isArray(res) ? res : (res.data || []);

  if (tarefas.length === 0) {
    lista.style.display = 'none';
    empty.style.display = 'block';
    return;
  }

  empty.style.display = 'none';
  lista.style.display = 'grid';

  const statusLabel = { pending: 'Pendente', in_progress: 'Em andamento', completed: 'Conclu\u00edda', overdue: 'Atrasada' };
  const statusColor = { pending: 'var(--info)', in_progress: 'var(--warning)', completed: 'var(--success)', overdue: 'var(--danger)' };

  lista.innerHTML = tarefas.map(t => {
    const s = t.status || 'pending';
    const deadline = t.deadline ? new Date(t.deadline).toLocaleDateString('pt-BR') : '—';
    const safeTitle = String(t.title || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    const safeLocation = String(t.location || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    return `<div style="background:#fff;border-radius:12px;padding:16px;box-shadow:0 2px 8px rgba(0,0,0,0.07);border-left:4px solid ${statusColor[s] || '#ccc'}">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px;">
        <div style="font-weight:700;font-size:0.9rem;color:var(--primary);">${safeTitle}</div>
        <span style="font-size:0.7rem;font-weight:700;padding:3px 8px;border-radius:999px;background:${statusColor[s] || '#ccc'};color:#fff;white-space:nowrap;">${statusLabel[s] || s}</span>
      </div>
      <div style="margin-top:6px;font-size:0.75rem;color:var(--text-light);">
        ${safeLocation ? '\u{1F4CD} ' + safeLocation + ' &bull; ' : ''}Prazo: ${deadline}
        ${t.xpReward ? ' &bull; ' + t.xpReward + ' XP' : ''}
      </div>
      ${s === 'completed' ? `<button onclick="verReportsTarefa('${String(t.id).replace(/'/g,"\\'")}', '${safeTitle}')" style="margin-top:10px;font-size:0.75rem;padding:5px 10px;background:var(--primary);color:#fff;border:none;border-radius:6px;cursor:pointer;">Ver Reports</button>` : ''}
    </div>`;
  }).join('');
}

async function verReportsTarefa(taskId, taskTitle) {
  const panel = document.getElementById('campo-reports-panel');
  const lista = document.getElementById('campo-reports-lista');
  panel.style.display = 'block';
  lista.innerHTML = '<p style="color:var(--text-light);font-size:0.85rem;">Carregando reports...</p>';
  panel.scrollIntoView({ behavior: 'smooth' });

  const reports = await ElexionClient.fetchTaskReports(taskId);
  if (!reports || reports.length === 0) {
    lista.innerHTML = '<p style="color:var(--text-light);font-size:0.85rem;">Nenhum report encontrado.</p>';
    return;
  }
  lista.innerHTML = reports.map(r => {
    const safeResult = String(r.result || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    const safeWorker = String(r.workerName || r.workerId || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    const completedAt = r.completedAt ? new Date(r.completedAt).toLocaleString('pt-BR') : '—';
    return `<div style="background:#f8f9fa;border-radius:10px;padding:14px;">
      <div style="font-size:0.8rem;font-weight:700;color:var(--primary);">${safeWorker}</div>
      <div style="font-size:0.78rem;color:var(--text);margin-top:4px;">${safeResult}</div>
      <div style="font-size:0.7rem;color:var(--text-light);margin-top:4px;">${completedAt}</div>
    </div>`;
  }).join('');
}

async function salvarTarefaCampo() {
  const titulo = document.getElementById('campo-task-titulo')?.value?.trim();
  const tipo = document.getElementById('campo-task-tipo')?.value;
  if (!titulo) { alert('T\u00edtulo obrigat\u00f3rio.'); return; }

  const btn = document.getElementById('campo-task-btn-salvar');
  if (btn) btn.disabled = true;

  const deadline = document.getElementById('campo-task-deadline')?.value;
  const payload = {
    title: titulo,
    type: tipo,
    description: document.getElementById('campo-task-descricao')?.value?.trim() || undefined,
    location: document.getElementById('campo-task-localizacao')?.value?.trim() || undefined,
    deadline: deadline ? new Date(deadline).toISOString() : undefined,
    xpReward: parseInt(document.getElementById('campo-task-xp')?.value) || undefined,
  };

  const tarefa = await ElexionClient.createTask(payload);
  if (!tarefa) {
    alert('Erro ao criar tarefa. Verifique conexao com Elexion.');
    if (btn) btn.disabled = false;
    return;
  }

  const workerId = document.getElementById('campo-task-worker-id')?.value?.trim();
  if (workerId && tarefa.id) {
    await ElexionClient.assignTask(tarefa.id, workerId);
  }

  closeModal('modalTarefaCampo');
  // Limpar campos
  ['campo-task-titulo','campo-task-descricao','campo-task-localizacao','campo-task-deadline','campo-task-xp','campo-task-worker-id'].forEach(id => {
    const el = document.getElementById(id);
    if (el) el.value = '';
  });
  if (btn) btn.disabled = false;
  renderTarefasCampo();
}

// ---- Desafios ----

async function renderDesafios() {
  const lista = document.getElementById('desafios-lista');
  const empty = document.getElementById('desafios-empty');
  const offline = document.getElementById('desafios-offline');
  if (!lista) return;

  const res = await ElexionClient.fetchChallenges();

  if (res === null) {
    lista.style.display = 'none';
    empty.style.display = 'none';
    offline.style.display = 'block';
    return;
  }

  offline.style.display = 'none';
  const desafios = Array.isArray(res) ? res : [];

  if (desafios.length === 0) {
    lista.style.display = 'none';
    empty.style.display = 'block';
    return;
  }

  empty.style.display = 'none';
  lista.style.display = 'grid';

  lista.innerHTML = desafios.map(d => {
    const safeName = String(d.type || d.name || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    const pct = d.progress != null ? Math.min(100, Math.round(d.progress)) : null;
    const endDate = d.endsAt ? new Date(d.endsAt).toLocaleDateString('pt-BR') : '—';
    return `<div style="background:#fff;border-radius:12px;padding:18px;box-shadow:0 2px 8px rgba(0,0,0,0.07);">
      <div style="font-weight:700;font-size:0.95rem;color:var(--primary);margin-bottom:6px;">${safeName}</div>
      <div style="font-size:0.75rem;color:var(--text-light);margin-bottom:10px;">
        Meta: ${d.target || '—'} &bull; Recompensa: ${d.xpReward || '—'} XP &bull; T\u00e9rmino: ${endDate}
      </div>
      ${pct != null ? `<div style="background:#f0f2f5;border-radius:999px;height:8px;overflow:hidden;margin-bottom:10px;">
        <div style="height:100%;width:${pct}%;background:var(--accent);transition:width 0.4s;"></div>
      </div>
      <div style="font-size:0.72rem;color:var(--text-light);margin-bottom:10px;">${pct}% conclu\u00eddo</div>` : ''}
      <button onclick="verLeaderboardDesafio('${String(d.id).replace(/'/g,"\\'")}', '${safeName}')" style="font-size:0.75rem;padding:5px 10px;background:var(--primary);color:#fff;border:none;border-radius:6px;cursor:pointer;">Ver Leaderboard</button>
    </div>`;
  }).join('');
}

async function verLeaderboardDesafio(challengeId, nome) {
  const panel = document.getElementById('desafio-leaderboard-panel');
  const titulo = document.getElementById('desafio-lb-titulo');
  const listaLb = document.getElementById('desafio-lb-lista');
  panel.style.display = 'block';
  titulo.textContent = 'Leaderboard: ' + nome;
  listaLb.innerHTML = '<p style="color:var(--text-light);font-size:0.85rem;">Carregando...</p>';
  panel.scrollIntoView({ behavior: 'smooth' });

  const lb = await ElexionClient.fetchChallengeLeaderboard(challengeId);
  if (!lb || lb.length === 0) {
    listaLb.innerHTML = '<p style="color:var(--text-light);font-size:0.85rem;">Nenhum participante ainda.</p>';
    return;
  }
  listaLb.innerHTML = '<table class="data-table"><thead><tr><th>#</th><th>Cabo</th><th>XP</th><th>Progresso</th></tr></thead><tbody>' +
    lb.map((c, i) => {
      const safeName = String(c.nome || c.name || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
      return `<tr><td>${i + 1}</td><td>${safeName}</td><td>${c.xp || 0}</td><td>${c.progresso || c.progress || '—'}</td></tr>`;
    }).join('') + '</tbody></table>';
}

async function salvarDesafio() {
  const tipo = document.getElementById('desafio-tipo')?.value;
  const metrica = document.getElementById('desafio-metrica')?.value?.trim();
  const alvo = parseInt(document.getElementById('desafio-alvo')?.value);
  const xp = parseInt(document.getElementById('desafio-xp')?.value);
  const duracao = parseInt(document.getElementById('desafio-duracao')?.value);

  if (!metrica || !alvo || !xp || !duracao) { alert('Preencha todos os campos obrigat\u00f3rios.'); return; }

  const btn = document.getElementById('desafio-btn-salvar');
  if (btn) btn.disabled = true;

  const payload = { type: tipo, metric: metrica, target: alvo, xpReward: xp, durationDays: duracao };
  const desafio = await ElexionClient.createChallenge(payload);

  if (!desafio) {
    alert('Erro ao criar desafio. Verifique conexao com Elexion.');
    if (btn) btn.disabled = false;
    return;
  }

  closeModal('modalDesafio');
  ['desafio-metrica','desafio-alvo','desafio-xp'].forEach(id => {
    const el = document.getElementById(id);
    if (el) el.value = '';
  });
  if (btn) btn.disabled = false;
  renderDesafios();
}

// ---- Social ----

async function renderSocial() {
  const equipesLista = document.getElementById('social-equipes-lista');
  const empty = document.getElementById('social-empty');
  const offline = document.getElementById('social-offline');
  const tsEl = document.getElementById('social-ultima-atualizacao');
  if (!equipesLista) return;

  // Buscar ranking de equipes para obter teamIds
  const ranking = await ElexionClient.fetchTeamsRanking();
  if (ranking === null) {
    equipesLista.style.display = 'none';
    empty.style.display = 'none';
    offline.style.display = 'block';
    return;
  }

  offline.style.display = 'none';
  const equipes = Array.isArray(ranking) ? ranking : [];

  if (equipes.length === 0) {
    equipesLista.style.display = 'none';
    empty.style.display = 'block';
    return;
  }

  empty.style.display = 'none';
  equipesLista.style.display = 'grid';
  if (tsEl) tsEl.textContent = 'Atualizado: ' + new Date().toLocaleTimeString('pt-BR');

  // Buscar metricas sociais de cada equipe em paralelo (max 5 equipes)
  const primeiras = equipes.slice(0, 5);
  const metricas = await Promise.all(
    primeiras.map(e => ElexionClient.fetchSocialTeamMetrics(e.id || e.equipe))
  );

  equipesLista.innerHTML = primeiras.map((e, i) => {
    const m = metricas[i];
    const safeName = String(e.equipe || e.name || '').replace(/</g, '&lt;').replace(/>/g, '&gt;');
    if (!m) return `<div style="background:#fff;border-radius:12px;padding:18px;box-shadow:0 2px 8px rgba(0,0,0,0.07);">
      <div style="font-weight:700;color:var(--primary);">${safeName}</div>
      <div style="font-size:0.75rem;color:var(--text-light);margin-top:8px;">Dados sociais indispon\u00edveis</div>
    </div>`;
    return `<div style="background:#fff;border-radius:12px;padding:18px;box-shadow:0 2px 8px rgba(0,0,0,0.07);">
      <div style="font-weight:700;font-size:0.9rem;color:var(--primary);margin-bottom:10px;">${safeName}</div>
      <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;text-align:center;">
        <div style="background:#f0f2f5;border-radius:8px;padding:8px;">
          <div style="font-size:1.1rem;font-weight:800;color:var(--accent);">${m.posts || 0}</div>
          <div style="font-size:0.65rem;color:var(--text-light);">Posts</div>
        </div>
        <div style="background:#f0f2f5;border-radius:8px;padding:8px;">
          <div style="font-size:1.1rem;font-weight:800;color:var(--info);">${m.engagement || 0}</div>
          <div style="font-size:0.65rem;color:var(--text-light);">Engajamento</div>
        </div>
        <div style="background:#f0f2f5;border-radius:8px;padding:8px;">
          <div style="font-size:1.1rem;font-weight:800;color:var(--success);">${m.reach || 0}</div>
          <div style="font-size:0.65rem;color:var(--text-light);">Alcance</div>
        </div>
      </div>
    </div>`;
  }).join('');
}

// ---- Wiring com showPage ----
// Interceptar showPage para disparar render automatico das 3 novas secoes
// Padrao identico ao usado para gamificacao (showPage wrapper ja existente na fase 2)
(function wireCampoPages() {
  const _origShowPage = window.showPage;
  if (typeof _origShowPage !== 'function') return;
  window.showPage = function(page) {
    _origShowPage(page);
    if (page === 'tarefas-campo') renderTarefasCampo();
    if (page === 'desafios') renderDesafios();
    if (page === 'social') renderSocial();
  };
})();

// ---- Polling 60s para as 3 secoes (alem do polling existente de gamificacao) ----
setInterval(function() {
  if (typeof currentPage === 'undefined') return;
  if (currentPage === 'tarefas-campo') renderTarefasCampo();
  if (currentPage === 'desafios') renderDesafios();
  if (currentPage === 'social') renderSocial();
}, 60000);
// ===== FIM OPERACOES DE CAMPO =====
```

Atentar para o sanitizacao: todos os dados da API inseridos via innerHTML usam `.replace(/</g,'&lt;').replace(/>/g,'&gt;')` conforme demonstrado acima (preparacao para SEC-03 na Fase 5).
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const src = fs.readFileSync('CONECTA.html', 'utf8');
const checks = [
  'renderTarefasCampo',
  'renderDesafios',
  'renderSocial',
  'salvarTarefaCampo',
  'salvarDesafio',
  'verReportsTarefa',
  'verLeaderboardDesafio',
  'wireCampoPages',
  'ElexionClient.fetchTasks',
  'ElexionClient.createTask',
  'ElexionClient.createChallenge',
  'ElexionClient.fetchSocialTeamMetrics',
  'replace.*&lt',
];
const missing = checks.filter(s => {
  if (s.includes('.*')) return !new RegExp(s).test(src);
  return !src.includes(s);
});
if (missing.length) { console.error('MISSING:', missing); process.exit(1); }
console.log('OK — todas as funcoes e chamadas ElexionClient presentes');
"
    </automated>
  </verify>
  <done>
    - renderTarefasCampo() lista tarefas Elexion com filtros, mostra fallback offline.
    - salvarTarefaCampo() chama createTask() + assignTask() se workerId fornecido.
    - renderDesafios() lista desafios ativos com barra de progresso.
    - salvarDesafio() chama createChallenge().
    - renderSocial() busca metricas por equipe via fetchSocialTeamMetrics().
    - wireCampoPages() intercepta showPage() para render automatico.
    - Polling 60s ativo para as 3 secoes.
    - Nenhum dado da API inserido no DOM sem sanitizacao de &lt;/&gt;.
  </done>
</task>

</tasks>

<verification>
Verificacao final do plano completo:

1. `node -e "require('./js/elexion-client.js')"` — sem erro de sintaxe
2. HTML valido: 3 novas divs .page, 2 novos modais, 3 novos nav-items na sidebar
3. Funcoes renderTarefasCampo/renderDesafios/renderSocial chamam ElexionClient
4. Modais usam closeModal() padrao existente
5. Nenhuma chave localStorage com prefixo conectacelina_ adicionada (evitar conflito com Supabase)
6. Polling usa currentPage (variavel global ja existente no CONECTA)
</verification>

<success_criteria>
- Clicar em "Tarefas de Campo" na sidebar mostra lista de tarefas do Elexion (ou fallback offline)
- Clicar em "+ Nova Tarefa" abre modal, preencher e salvar chama POST /api/v1/tasks
- Tarefas concluidas exibem botao "Ver Reports" que busca GET /tasks/:id/reports
- Clicar em "Desafios" mostra desafios ativos com barra de progresso
- Clicar em "+ Novo Desafio" abre modal e salvar chama POST /api/v1/challenges
- Clicar em "Social" mostra cards de metricas por equipe (posts, engagement, alcance)
- Quando Elexion offline: as 3 secoes exibem mensagem de fallback em vez de erro JS
- Nenhuma funcao existente do CONECTA foi quebrada (tarefas Supabase continuam funcionando)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/04-operacoes-de-campo-tarefas-desafios-social/04-01-SUMMARY.md` com:
- Metodos adicionados ao ElexionClient
- Secoes criadas no CONECTA.html
- Modais criados
- Decisoes de implementacao (sanitizacao, wireCampoPages pattern, polling)
- Qualquer divergencia encontrada nos endpoints Elexion durante execucao
</output>
